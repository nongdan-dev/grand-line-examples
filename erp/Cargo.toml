[package]
name = "erp"
version = "0.0.0"
edition = "2021"

[dependencies]
# grand_line
grand-line = { path = "../../grand-line", features = ["sqlite", "axum", "tracing"] }
serde = { version = "1.0.210", default-features = false, features = ["derive", "std"] }
