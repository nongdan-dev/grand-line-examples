[package]
name = "grand-line-examples-relation"
version = "0.0.0"
edition = "2024"

[dependencies]
grand-line = { path = "../../grand-line", default-features = false, features = ["axum", "sqlite", "tracing"] }
serde = { version = "1.0.219", default-features = false, features = ["derive", "std"] }
